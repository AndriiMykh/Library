import { NotWhitspacesValidator } from './not-whitspaces-validator';

describe('NotWhitspacesValidator', () => {
  it('should create an instance', () => {
    expect(new NotWhitspacesValidator()).toBeTruthy();
  });
});
